# Posecode Cut 2 — Builder video

Remotion source for the 28-second, seven-beat MCP/agent cut.

```bash
npm run video:cut2
npm run video:cut2:vertical
npm run video:cut2:still
```

Outputs are written to `docs/launch-media/`.
